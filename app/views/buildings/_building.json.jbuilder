json.extract! building, :id, :roof_set_id, :name, :slug, :description, :district, :address, :city, :state, :zipcode, :latitude, :longitude, :roof_sq_feet, :sunlight_hours, :utility, :created_at, :updated_at
json.url building_url(building, format: :json)
